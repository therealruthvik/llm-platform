# Lightweight deps for CI unit tests (no torch/transformers – model is mocked)
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
prometheus-client==0.20.0
httpx==0.27.0
pytest==8.2.0
pytest-asyncio==0.23.7
python-multipart==0.0.9
huggingface-hub==0.27.0
# Stub heavy packages so imports don't fail at collection time
# (model_manager imports transformers – mock patches before import)
