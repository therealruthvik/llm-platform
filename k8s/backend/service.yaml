apiVersion: v1
kind: Service
metadata:
  name: llm-backend
  namespace: llm-platform
  labels:
    app: llm-backend
spec:
  selector:
    app: llm-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
